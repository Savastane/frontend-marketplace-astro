---
import LogoHomeComponent from '../components/LogoHomeComponent.astro';
import HeaderSearchComponent from '../components/HeaderSearchComponent.astro';
import HeaderNavComponent from '../components/HeaderNavComponent.astro';
import HeaderAuthComponent from '../components/HeaderAuthComponent.astro';
---

<header class="main-header">
    <!-- logo-->

    <LogoHomeComponent />
    <!-- <HeaderSearchComponent /> -->
    <HeaderNavComponent />
	<HeaderAuthComponent />
       

</header>

<!-- <style>
.main-header {
  @apply fixed w-full h-20 z-[31] px-10 py-0 left-0 top-0 before:content-[''] before:absolute before:w-[480px] before:h-full before:opacity-100 before:transition-all before:duration-[0.3s] before:ease-[ease-in-out] before:right-0 before:bottom-0;
  background: #2e3f6e;
  /* background:#8d1601; */
  /* background: #7B68EE;  */
  -webkit-transform: translate3d(0, 0, 0);
}
.main-header:before {
  background: #3d528b;
  /* background: #9370DB;  */
  /* 9370DB */
  /* background: #b48502; */
}
.dsh-header.main-header:before {
  @apply w-[540px];
}
.main-header.vis-searchdec:before {
  @apply opacity-0;
}

</style>  -->